# 快手登录+发布 依赖。装在 conda313 里(本机基线)。
cloakbrowser>=0.3.32      # 指纹浏览器(C++级真指纹,一号一设备)；本机 conda313 已装
curl_cffi>=0.15.0         # 素材直传用的 Chrome TLS/JA3 指纹 HTTP 客户端
pillow>=10.0.0            # 量图片真实宽高/大小
# opencv-python           # 可选：探视频宽高/时长/帧率(装了发布体更贴真机；没装退占位)
