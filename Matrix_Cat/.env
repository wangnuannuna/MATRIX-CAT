# Matrix_Cat 本地配置(密钥别进 git)。改完重启后端生效。
# AI 文案优化:默认走国内免费的智谱 GLM-4-Flash,只需填这把 key。
MATRIXCAT_AI_PROVIDER=zhipu
MATRIXCAT_AI_KEY=241dfb5118fe430982261c4cd8189e21.2rIQbL5SF5Lpsoof

# 发布模式:1=模拟(默认,防误发,会显示假"成功");0=真发到平台。
# 真发前提:①号已真扫码登录(有真实登录态)②worker 在跑。
MATRIXCAT_DRYRUN=0

# 小红书发布走【纯协议】(NodeSigner 出 X-s/X-t/X-S-Common + curl 直发,不开浏览器)。
# 实测:纯协议【签名/读/permit/上传】全通;但【create_note 写】即便真UA+JA3自洽+新鲜独立IP 仍被风控软封
# (461,行为/会话信任层,非指纹)。故默认 0=写走浏览器路(能发成功);纯协议留作读/签名加速。
XHS_PURE_PROTO=0
