# Matrix_Cat 目录说明

> 整理时间:2026-07-22。本文件放在仓库最外层,说清每个目录是干什么的。

最外层一共三块东西:**主项目(Matrix_Cat/)**、**云端鉴权服务(platform_server/)**、**便携版 Redis(redis-portable/)**。

```
Matrix_Cat/                  ← 你在这里(最外层)
├─ Matrix_Cat/                 主项目:多平台爬虫 / 自动发布 + SaaS 后端(Python)
├─ platform_server/            云端鉴权服务:用户账号 + 会员授权(独立于主项目)
└─ redis-portable/             Windows 便携版 Redis(免安装,给后端队列/缓存用)
```

---

## 一、Matrix_Cat/(主项目)

多平台(小红书 / 抖音 / 快手 / B站)自动发布工具。协议直连(curl_cffi + 自算签名),登录用 CloakBrowser 指纹浏览器。

### 入口文件(这层目录下直接能跑的)

| 文件 | 作用 |
|---|---|
| `启动.bat` | 双击入口 → `run.py`,起网页总控台 http://127.0.0.1:8799 |
| `run.py` | 整体启动入口:默认起总控台网页;也能转发到某平台命令行(如 `python run.py xhs login <账号>`) |
| `启动后端.bat` | 双击入口 → `run_backend.py` |
| `run_backend.py` | 一键起后端全家桶:Redis + 发布 worker + 定时调度 + FastAPI API |
| `README.md` / `项目结构.md` / `架构图.html` | 项目说明文档(注:`项目结构.md` 是 7 月 12 日的旧版,部分结构已演进) |
| `logo.png` | 项目 logo |

### 各子目录

```
Matrix_Cat/Matrix_Cat/
├─ frontend/          【前端】纯静态网页,只打后端 /api/*
│  ├─ index.html        网页总控台 UI(选号 → 填内容 → 发布)
│  ├─ console.html      控制台页
│  ├─ account.html      账号页
│  ├─ admin.html        管理页
│  ├─ beacon.html       埋点/回跳页
│  └─ assets/           前端静态资源(logo、favicon、图标)
│
├─ backend/           【后端】FastAPI 主后端 + 旧版 aiohttp 总控台
│  ├─ app.py            FastAPI 主应用:账号/任务/发布 API + 托管前端(uvicorn backend.app:app)
│  ├─ server.py         旧版 aiohttp 总控测试台(run.py 默认起的就是它,平台适配器注册表 _ADAPTERS)
│  ├─ api/              FastAPI 路由层:auth / accounts / platforms / publish / tasks / membership / ai / admin
│  ├─ jobs/             任务队列层(Dramatiq + Redis):
│  │  ├─ broker.py        Redis 消息队列配置
│  │  ├─ tasks.py         发布 worker:取任务 → 调采集层发布 → 回写成功/失败 → 重试
│  │  ├─ collector.py     桥到采集层执行真发布(默认 DRY_RUN 模拟,MATRIXCAT_DRYRUN=0 才真发)
│  │  └─ login.py         扫码登录作业:worker 起 CloakBrowser 弹二维码,登录态加密写回 DB
│  ├─ scheduler.py      定时发布调度器(每 5 秒扫到点任务推进队列)
│  ├─ ai.py             AI 文案优化(默认智谱 GLM-4-Flash,可换多家供应商)
│  ├─ payments.py       支付网关抽象(mock / 支付宝当面付;微信预留)
│  ├─ models.py / db.py / crud.py / schemas.py   SQLAlchemy 模型 / 数据库会话 / 增删改查 / 请求响应模型
│  ├─ auth.py / security.py   认证与密码/JWT 安全
│  ├─ seed.py / migrate_accounts.py   初始化数据 / 老账号数据迁移进 DB
│  └─ config.py         后端配置(环境变量覆盖)
│
├─ collectors/        【采集层】每平台一个 <平台>_publisher/(登录 + 发布)
│  ├─ xhs_publisher/      小红书:xhs/ 平台包(config/login/signer/cos/note/publish)+ sign/(签名 JS)
│  ├─ douyin_publisher/   抖音:douyin/ 平台包 + sign/(probe_bdms.js 签名探针)
│  ├─ kuaishou_publisher/ 快手:kuaishou/ 平台包
│  ├─ bilibili_publisher/ B站:bilibili/ 平台包
│  ├─ _template/          新平台起手模板(pkg/ 复制改名即用)
│  └─ <平台>/data/        各平台自己的账号数据:登录态、每号一份 CloakBrowser 指纹 profiles/、二维码 qr/  ← 不进 git
│
├─ asset_cache/       【静态资源缓存】解决"登录慢半天不出码"
│  ├─ cache.py          数据面:各平台登录/发布页的 JS/CSS 首次走代理下载后存本地磁盘,
│  │                    之后命中本地直接喂浏览器、绕过代理;顺带屏蔽吃带宽的重资源
│  ├─ login/prewarm.py  预热 CLI:直连先把登录页静态资源灌进缓存,真机扫码秒出码
│  └─ _store/           缓存落盘目录(login/ 与 publish/ 分开存)
│
├─ shared/            【公共层】跨层复用
│  ├─ browser.py        CloakBrowser 会话底座:源码级改指纹的隐身 Chromium,一号一设备一指纹
│  ├─ http.py           curl_cffi 会话工厂 + 代理路由(境内直连 / 海外走 VPN)
│  ├─ signing.py        签名原语(sha256 / hmac / hkdf / ecdh-p256 / aws4 / base64)
│  ├─ io_utf8.py        Windows utf-8 控制台 + 统一 logging
│  └─ paths.py          data/ 目录约定
│
├─ storage/           【存储】
│  ├─ session_store.py  通用 AccountStore(json 落盘 + 按 user_id 去重 + 风控冷却)
│  └─ data/             matrixcat.db(主项目 SQLite 库)+ uploads/(网页上传的临时素材)  ← 不进 git
│
└─ config/            【全局配置】
   └─ settings.py       UA / CloakBrowser / curl 指纹 / 代理(只收北京出口)/ 超时——集中一处,别处只 import
```

**数据流(一次发布):** 浏览器(frontend)→ `backend` API → 存素材到 `storage/data/uploads/` → `jobs` 队列 → `collectors/<平台>_publisher` 校验账号态、传素材、加签发布 → 结果一路回传网页。

---

## 二、platform_server/(云端鉴权服务)

矩阵猫 SaaS 的**用户账号 + 会员授权**后端,独立于本地发布工具,只管"谁能用、用到什么档"。FastAPI 写成,默认 SQLite + 内存兜底开箱即跑,上线换 PostgreSQL + Redis 即可。

```
platform_server/
├─ app/                FastAPI 应用
│  ├─ main.py            应用入口(uvicorn app.main:app --port 8800)
│  ├─ config.py          配置(env 覆盖)
│  ├─ db.py / models.py  异步 SQLAlchemy 引擎 / User、Plan、Subscription、SocialAccount 等模型
│  ├─ security.py        argon2 密码哈希 + JWT + refresh token
│  ├─ store.py           Redis 或内存(验证码/频控存储)
│  ├─ sms.py             短信发送(Console 开发版 / Aliyun)+ 频控
│  ├─ schemas.py / deps.py / audit.py / utils.py   请求响应模型 / 鉴权依赖 / 审计日志 / 工具
│  └─ routers/           路由:auth.py(注册登录令牌)/ account.py(绑社媒号)/ billing.py(套餐订阅)
├─ init_db.py          建表 + 初始化套餐
├─ smoke_test.py       端到端冒烟自测(11 步)
├─ requirements.txt    依赖清单
├─ matrixcat.db        本地 SQLite 库(开发用)
├─ .env.example        配置样例
└─ .venv/              它自己的 Python 虚拟环境
```

能力:手机号+验证码注册登录、账号密码登录、JWT access + 可轮换 refresh、会员分档(体验/基础/专业/旗舰,按可绑账号数限配额)、防爆破/限流/防越权/审计。细节见它自己的 `README.md`。

---

## 三、redis-portable/(便携版 Redis)

Windows 免安装版 Redis(tporadowski 的 Windows 移植版),给两块后端当消息队列和缓存用。双击 `redis-server.exe` 就能起,不用装服务。

- `redis-server.exe` — Redis 服务端(主程序)
- `redis-cli.exe` — 命令行客户端
- `redis-benchmark.exe` / `redis-check-aof.exe` / `redis-check-rdb.exe` — 压测 / 持久化文件检查工具
- `redis.windows.conf` / `redis.windows-service.conf` — 配置文件(普通运行 / 注册为 Windows 服务)
- `redis.zip` — 原始压缩包(来源备份)
- 其余 `*.pdb` 是调试符号文件,`00-RELEASENOTES`、`RELEASENOTES.txt`、`README.txt` 是版本说明

主项目 `run_backend.py` 和 `platform_server` 默认都会找 `127.0.0.1:6379`;没起 Redis 时 platform_server 自动退内存版(仅开发够用),主后端的发布队列则必须有 Redis。
